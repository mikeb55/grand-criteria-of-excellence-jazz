# Open Triad Engine v1.0 Dependencies

# Core dependencies (all in standard library)
# No external dependencies required for core functionality

# Optional: For testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: For enhanced PDF generation
# weasyprint>=60.0  # Uncomment if PDF export is needed

# Optional: For MIDI file support
# mido>=1.3.0  # Uncomment for MIDI export

